# Number guessing game 
A small Python program where the user guesses a random number. The program gives feedback on whether the guess is too high or too low and keeps running until the correct number is guessed. I built this to practice basic Python logic like loops and if statements.
